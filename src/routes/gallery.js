import React from 'react'

import GalleryForm from '../components/GalleryForm'

const Gallery = () => (
  <div>
    <h1>ให้คะแนนรูปวาด (ข้อ 3) สาขาดีไซน์</h1>

    <GalleryForm />
  </div>
)

export default Gallery
